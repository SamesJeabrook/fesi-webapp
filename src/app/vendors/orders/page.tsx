'use client';

import { CustomerOrdersTemplate } from '@/components/templates';

// Customer orders page - displays order history and status
export default function CustomerOrdersPage() {
  return <CustomerOrdersTemplate showNavigation={true} />;
}
